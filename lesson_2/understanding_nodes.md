# Туториал: понимание нод (Nodes) в ROS 2

(по материалам официальной документации ROS 2 Humble)

---

## Что такое ROS 2 graph и ноды

### ROS 2 graph

ROS 2 graph — это совокупность **всех нод**, которые в данный момент запущены в системе, и **всех соединений между ними** (topics, services, actions, parameters).
Иначе говоря, это логическая схема всей ROS-системы «здесь и сейчас».

![Image](images/Nodes-TopicandService.gif)

![Image](images/ROS-graph-concepts-a-Core-computation-graph-level-b-messages-publish-and-subscribe.png)

На схеме обычно показано:

* несколько нод;
* топики, по которым они обмениваются сообщениями;
* направления передачи данных.

---

### Ноды (Nodes)

**Нода** — это базовая исполняемая единица ROS 2, выполняющая **одну логически законченную задачу**:

* управление приводами;
* чтение сенсоров;
* навигация;
* визуализация и т.д.

Ноды **общаются друг с другом**, но не знают внутренней реализации других нод — только интерфейсы.

![Image](images/Nodes-TopicandService.gif)

![Image](images/Topic-SinglePublisherandSingleSubscriber.gif)

💡 Важно:

* один исполняемый файл может содержать **одну или несколько нод**;
* ноды можно свободно комбинировать и заменять без изменения всей системы.

---

## Подготовка к работе

Перед выполнением команд:

1. Установи пакет `turtlesim`
2. В каждом новом терминале выполняй:

```bash
source /opt/ros/humble/setup.bash
```

---

## Практика: запуск и исследование нод

### Шаг 1. Запуск ноды turtlesim

Запускаем исполняемый файл из пакета:

```bash
ros2 run turtlesim turtlesim_node
```

Результат — графическое окно симулятора черепахи.

![Image](images/turtlesim.png)

![Image](images/ros2-turtlesim-06.webp)

На этом этапе:

* нода **уже запущена**
* но её имя мы пока не смотрели явно

---

### Шаг 2. Просмотр списка нод

В новом терминале:

```bash
ros2 node list
```

Ожидаемый вывод:

```text
/turtlesim
```

![Image](images/6675ebfd49a56adc7dcdef8a6731efa91292d988.png)

![Image](images/rqt.png)

📌 Команда `ros2 node list` — основной инструмент диагностики:

* проверка, что нода реально запущена;
* поиск имени ноды для дальнейшей работы.

---

### Шаг 3. Добавляем вторую ноду (teleop)

Запускаем управление с клавиатуры:

```bash
ros2 run turtlesim turtle_teleop_key
```

Теперь снова:

```bash
ros2 node list
```

Результат:

```text
/turtlesim
/teleop_turtle
```

![Image](images/image_05_001.jpg)

![Image](images/remap.png)

Теперь ROS 2 graph состоит из **двух нод**, соединённых через топик `/turtle1/cmd_vel`.

---

## Remapping имени ноды

Remapping позволяет **переопределять параметры запуска**, включая имя ноды.

Запускаем вторую черепаху с другим именем:

```bash
ros2 run turtlesim turtlesim_node --ros-args --remap __node:=my_turtle
```

Теперь список нод:

```bash
ros2 node list
```

```text
/turtlesim
/my_turtle
/teleop_turtle
```

![Image](images/Nodes-TopicandService.gif)

![Image](images/150209742-bb8e8bf6-e624-4c75-a933-f8ae2560f1b4.png)

📌 Это важно для:

* запуска нескольких одинаковых нод;
* масштабируемых систем;
* симуляций и роя роботов.

---

## Интроспекция ноды

### Команда `ros2 node info`

Позволяет увидеть **все связи конкретной ноды**.

Пример:

```bash
ros2 node info /my_turtle
```

Вывод включает:

* Publishers
* Subscribers
* Service Servers
* Service Clients
* Action Servers / Clients

![Image](images/Topic-SinglePublisherandSingleSubscriber.gif)

![Image](images/turtlesim.png)

Это фактически «паспорт ноды» в рамках ROS 2 graph.

---

## Что важно понять после туториала

✔ Нода — это автономный модуль
✔ ROS 2 graph — динамическая сеть нод
✔ `ros2 node list` — поиск нод
✔ `ros2 node info` — анализ связей
✔ Remapping позволяет гибко управлять архитектурой

---

## Логичное продолжение

Следующий шаг — **topics**, потому что именно они связывают ноды в работающую систему.